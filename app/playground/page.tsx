import AppButton from "../components/AppButton";
import { RefreshCcw } from "lucide-react";

const playgroundPage = () => {
  return (
    <div className="p-10">
      <AppButton icon={RefreshCcw}>Button</AppButton>
    </div>
  );
};

export default playgroundPage;
